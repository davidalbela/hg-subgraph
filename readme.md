# Hg Subgraph

Subgraph for Gnosis Conditional Tokens

---

## Require

- Docker CE
- docker-compose
- Node `v12.9.1` or higher

## Testing Quickstart

1.  Clone this repo.
2.  `docker-compose up -d`
3.  `npm i && npm test`
